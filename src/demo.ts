type UserId = string | number;
let userId: UserId;
userId = "string"
userId = 1
console.log("ESTOY FUNCIONANDO")


/*
ts-node es una herramienta que permite ejecutar y depurar archivos TS directamente en Node.js sin necesidad de compilarlos previamente a JS.Para configurar un proyecto con ts-node, primero debes asegurarte de tener Node.js y TypeScript instalados en tu sistema.
*/
